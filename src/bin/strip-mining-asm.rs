// #![feature(asm)]
/*
    println!("=== Begin with n={} ===", n);
    let mut lo = 0;
    for _ in 0..n/L {
        println!("Processing {} to {}", lo, lo + L - 1);
        lo += L;
    }
    for i in lo..n {
        println!("Processing {}", i);
    }
*/

fn main() {

}
